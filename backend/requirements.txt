# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0          # async driver (FastAPI)
psycopg2-binary==2.9.9   # sync driver (Celery worker)
alembic==1.13.2

# Validation
pydantic==2.9.2
pydantic-settings==2.5.2

# Task queue
celery==5.4.0
redis==5.1.0

# Scraping — core
playwright==1.47.0

# Scraping — parsing
beautifulsoup4==4.12.3
lxml==5.3.0

# HTTP (lightweight requests when no JS needed)
httpx==0.27.2

# Utilities
python-dotenv==1.0.1
tenacity==9.0.0        # retry logic
fake-useragent==1.5.1  # UA rotation

# Dev
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2          # test client
