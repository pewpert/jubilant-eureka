[pytest]
# Only collect the real offline unit suite. test_filters.py / test_scraper.py at
# the backend root are manual live-network scripts (run via `python test_filters.py`),
# not pytest tests — scoping to tests/ keeps `pytest` green without renaming them.
testpaths = tests
asyncio_mode = auto
